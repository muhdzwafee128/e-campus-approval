# Node modules
node_modules/
client/node_modules/
server/node_modules/

# Environment variables — NEVER commit secrets
.env
server/.env
client/.env

# Build / dist files
dist/
build/
client/dist/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE / editor files
.vscode/
.idea/
*.swp
*.swo

# ─── Local file storage (migrated to Cloudinary) ─────────────────────────────
# All three folders below are now replaced by Cloudinary cloud storage.
# Do NOT commit these — they are large, machine-specific, and redundant.

# Student attachment uploads (images, PDFs, DOCX)
server/uploads/

# Staff signature images
server/uploads/signatures/

# Generated permission-letter PDFs
server/pdfs/

# Legacy standalone signatures folder (if it exists)
server/signatures/
